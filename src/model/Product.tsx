export type Product = {
  activationDateTime?: Date;
  description?: string;
  id: string;
  logo?: string;
  title: string;
  urlBO?: string;
  urlPublic?: string;
  tag?: string;
  authorized?: boolean;
  active:boolean;
};
