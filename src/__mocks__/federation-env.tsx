const FederationMock = () => {
    return <div data-testid="federation-mock" />;
};

export default FederationMock;

export const RoutingAdmin = FederationMock;
export const RoutingUsers = FederationMock;
export const RoutingGroups = FederationMock;
export const RoutingProductUsers = FederationMock;
